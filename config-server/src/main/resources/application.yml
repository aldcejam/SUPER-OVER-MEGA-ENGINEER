server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aldcejam/SUPER-OVER-MEGA-ENGINEER.git
          search-paths: config-server/src/main/resources/config
          default-label: main
          clone-on-start: true