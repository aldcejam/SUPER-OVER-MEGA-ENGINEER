server:
  port: 8086

spring:
  cloud:
    function:
      definition: lambdaService
  datasource:
    url: jdbc:postgresql://localhost:5433/supersys_vector_db
    username: apm
    password: super_password_2026
  ai:
    vectorstore:
      pgvector:
        table-name: vector_store
        dimension: 384
        initialize-schema: true

services:
  ai-service:
    url: ${AI_SERVICE_URL:http://localhost:8082}
  lambda-service:
    url: ${LAMBDA_SERVICE_URL:http://localhost:8086}
