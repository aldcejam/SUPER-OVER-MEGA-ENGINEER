server:
  port: 8083

spring:
  application:
    name: empire-manager-erp
  datasource:
    url: jdbc:postgresql://postgres:5432/supersys-relational-db
    username: apm
    password: super_password_2026
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  graphql:
    graphiql:
      enabled: true
      path: /graphiql


management:
  endpoints:
    web:
      exposure:
        include: "*"
